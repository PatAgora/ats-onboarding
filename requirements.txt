# Core Framework
Flask==3.0.3
Flask-Login==0.6.3
Flask-WTF==1.2.1
WTForms==3.1.2
SQLAlchemy==2.0.34
email-validator==2.2.0
python-dotenv==1.0.1
gunicorn==23.0.0
requests==2.32.3
python-dateutil==2.9.0.post0

# Security Enhancements - CREST Compliance
Flask-Talisman==1.1.0          # HTTPS enforcement, security headers
Flask-Limiter==3.5.0           # Rate limiting for brute-force protection
pyotp==2.9.0                   # TOTP for 2FA/MFA
qrcode==7.4.2                  # QR codes for MFA setup
Pillow==10.1.0                 # Required for qrcode
itsdangerous==2.1.2            # Secure tokens for magic links (already used)

# Database Support
psycopg2-binary==2.9.9         # PostgreSQL support for Railway
alembic==1.13.1                # Database migrations

# Monitoring & Error Tracking
sentry-sdk[flask]==1.39.2      # Error monitoring and alerting

# Security & Audit
python-decouple==3.8           # Better environment variable management
cryptography==41.0.7           # Encryption utilities

# PDF/Document Processing (for CV parsing)
pdfplumber==0.10.3             # PDF text extraction
python-docx==1.1.0             # DOCX parsing

# File Type Validation
python-magic==0.4.27           # MIME type detection
filetype==1.2.0                # File type validation

# E-sign SDKs (optional)
dropbox-sign==1.10.0
docusign-esign==4.0.0

# HTTP Client with better security
httpx==0.25.2                  # Modern HTTP client for OpenAI

# Additional utilities
bleach==6.1.0                  # HTML sanitization
MarkupSafe==2.1.3              # XSS protection
